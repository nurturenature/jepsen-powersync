name: powersync_endpoint
description: A Jepsen endpoint backend by PowerSync, and a local CLI fuzzer for PowerSync.
version: 0.0.1
repository: https://github.com/nurturenature/jepsen-powersync

environment:
  sdk: ^3.7.0

dependencies:
  args: ^2.6.0
  http: ^1.3.0
  jose: ^0.3.4
  list_utilities: ^1.0.3
  logging: ^1.2.0
  postgres: ^3.5.4
  powersync: ^1.11.3
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
